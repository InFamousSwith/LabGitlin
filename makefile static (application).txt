LIBS = -L../library -lfunctions
CPPFLAGS = -I ../library
TARGET   = decoderstat

all: $(TARGET)

$(TARGET): main.o
	g++ -Wall -W  -o $(TARGET) main.o -L../library -lfunctions -static #-Wl,-rpath=../library

clean:
	rm -f $(OBJFILES) $(TARGET) *~